version: 1.0.0
variables:
  global:
    BASIC_AUTH_USER: Something
    BASIC_AUTH_PASS: SomethingPass
events:
  build:
    steps:
      - install:
          script:
            - 'echo "install step"'
      - test:
          script:
            - 'echo "test step"'
      - deploy:
          script:
            - 'echo "deploy step"'
